// 差分垫片:真身 `SourceVerificationHelp` 拉起来的内置浏览器界面。
// 差分里它只当**类型**用 —— `startActivity<T>` 的垫片把 extras 交给
// `jsharness.VerifyStage`,由剧本用户替人答一句(契约见
// fixtures/cases/js-host/README.md 的 `verify` 一节)。
@file:Suppress("unused")

package io.legado.app.ui.browser

class WebViewActivity
