[package]
name = "reader-layout"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# 分页器。**不依赖 Flutter、FRB、数据库和网络**(计划书 §4.1):
# 测量结果由调用方喂进来,单测直接造行度量,不需要起 UI。
[dependencies]
reader-doc = { path = "../reader-doc" }

[lints]
workspace = true
