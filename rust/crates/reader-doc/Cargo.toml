[package]
name = "reader-doc"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# 正文文档模型:不依赖网络、数据库、FFI 与 Flutter。
# 单测只喂字符串,不需要起引擎(计划书 §4.1)。
[dependencies]

[lints]
workspace = true
