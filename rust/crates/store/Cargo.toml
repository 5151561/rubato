[package]
name = "store"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
rubato-core = { path = "../rubato-core" }
# bundled:不依赖系统 sqlite,Android/桌面交叉编译一致
rusqlite = { version = "0.32", features = ["bundled"] }
serde_json.workspace = true

[lints]
workspace = true
